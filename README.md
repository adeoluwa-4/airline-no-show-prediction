# Airline Passenger Satisfaction Predictor

## What it does

This app predicts whether an airline passenger will be satisfied with a flight. A user enters trip and service details, then gets a simple prediction.

## What I built

I built the data cleaning, charts, Random Forest model, and Streamlit app in Python. Run it with `streamlit run app.py` after installing the packages in `requirements.txt`.
